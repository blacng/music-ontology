[project]
name = "music-ontology"
version = "0.1.0"
description = "A gist-aligned OWL 2 music ontology for discovery/recommendation, built with the GRL Workshop methodology."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyshacl>=0.31.0",
    "rdflib>=7.6.0",
]
